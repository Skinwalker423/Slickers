import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Navbar } from "@/components/layout/Navbar";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Slickers",
  description:
    "Are you drained from the new normal? Find new soul regenerating experiences provided by real people living different ways of life in natural environments. You won't be experiencing glamping, tour guides, or tourist traps adventures. You will be living a new life from a another perspective.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body className={inter.className}>
        <Navbar />
        {children}
      </body>
    </html>
  );
}
